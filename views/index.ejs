<% const body = `
<div class="hero">
  <h1>🎅 Secret Santa 🎄</h1>
  <p class="subtitle">Organisez votre échange de cadeaux en toute simplicité !</p>

  <div class="stats">
    <div class="stat-box">
      <span class="stat-number">${participantCount}</span>
      <span class="stat-label">Participant${participantCount > 1 ? 's' : ''} inscrit${participantCount > 1 ? 's' : ''}</span>
    </div>
  </div>

  <div class="cta-buttons">
    <a href="/register" class="btn btn-primary">S'inscrire</a>
  </div>
</div>

<section class="how-it-works">
  <h2>Comment ça marche ?</h2>
  <div class="steps">
    <div class="step">
      <div class="step-number">1</div>
      <h3>Inscrivez-vous</h3>
      <p>Remplissez le formulaire avec vos informations et vos idées de cadeaux.</p>
    </div>
    <div class="step">
      <div class="step-number">2</div>
      <h3>Attendez le tirage</h3>
      <p>L'organisateur effectuera le tirage au sort une fois tout le monde inscrit.</p>
    </div>
    <div class="step">
      <div class="step-number">3</div>
      <h3>Recevez votre email</h3>
      <p>Vous recevrez un email secret vous indiquant à qui offrir un cadeau.</p>
    </div>
  </div>
</section>
` %>
<%- include('layout', { title, isAdmin, body }) %>
